<%= form_for(@protesto) do |f| %>
  <% if @protesto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@protesto.errors.count, "error") %> prohibited this protesto from being saved:</h2>

      <ul>
      <% @protesto.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pessoa %><br>
    <%= f.text_field :pessoa %>
  </div>
  <div class="field">
    <%= f.label :descricao %><br>
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
